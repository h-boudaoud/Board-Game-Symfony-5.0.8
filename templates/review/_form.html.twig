{#{% form_theme form 'bootstrap_4_layout.html.twig' %}#}


<div class="container">
    <div class=" ajaxForm">

        {{ form_start(form) }}
        {{ form_widget(form) }}

        <button type ="submit" class="btn btn-inverse">{{ review_form_button_label|default('Save') }}</button>
        {{ form_end(form) }}

    </div>
    <div class="my-3">
        <a href="{{ path('review_byGame', {'id': game.id, 'name':game.name}) }}" class="btn btn-inverse">
            Reviews list
        </a>
        {% if is_granted("ROLE_ADMIN") and review.id is defined and review.id>0 %}
            {{ include('review/_delete_form.html.twig') }}
        {% endif %}
    </div>
</div>

