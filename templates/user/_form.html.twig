{% form_theme form 'bootstrap_4_layout.html.twig' %}

<div class="container">
    {{ form_start(form) }}
    <div id="ajaxForm">
        {{ form_widget(form) }}
        <button class="btn btn-inverse" type="submit">{{ button_label|default('Save') }}</button>
    </div>
    {{ form_end(form) }}
</div>
