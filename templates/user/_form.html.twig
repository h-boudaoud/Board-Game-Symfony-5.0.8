{% form_theme form 'bootstrap_4_layout.html.twig' %}

<div class="container">

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-inverse">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
</div>
